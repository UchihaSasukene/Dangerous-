<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.vueones.mapper.UserMapper">

    <resultMap id="BaseResultMap" type="com.vueones.entity.User">
        <id column="id" property="id"/>
        <result column="email" property="email"/>
        <result column="name" property="name"/>
        <result column="password" property="password"/>
        <result column="user_type" property="userType"/>
        <result column="status" property="status"/>
        <result column="last_login_time" property="lastLoginTime"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <sql id="BaseColumns">
        id, email, name, password, user_type, status, last_login_time, create_time, update_time
    </sql>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT
        <include refid="BaseColumns"/>
        FROM user_account
        WHERE id = #{id}
    </select>

    <select id="selectByEmail" resultMap="BaseResultMap">
        SELECT
        <include refid="BaseColumns"/>
        FROM user_account
        WHERE email = #{email}
        LIMIT 1
    </select>

    <insert id="insert" parameterType="com.vueones.entity.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user_account
        (email, name, password, user_type, status, create_time, update_time)
        VALUES
        (#{email}, #{name}, #{password}, #{userType}, #{status}, NOW(), NOW())
    </insert>

    <update id="updateLastLoginTime">
        UPDATE user_account
        SET last_login_time = NOW(),
            update_time = NOW()
        WHERE id = #{id}
    </update>

    <insert id="insertRegisterRecord">
        INSERT INTO user_register_record
        (email, name, user_type, register_ip, register_channel, create_time)
        VALUES
        (#{email}, #{name}, #{userType}, #{registerIp}, #{registerChannel}, NOW())
    </insert>
</mapper>

